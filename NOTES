## 1 Sending Get Request

- Implemented
  => "App.js" (get data using "async/await", using data with "useState")

--------------------------------------------- fetch('link')
Default for "Get" api

//////////////////////////////////////////////////////////////////////////////////////

## 2 Handling Loading & Data States

- implemented
  => "App.js" (useState(true/false) to set loading message "before/after" "async/await" data)
  Handling Data in JSX with "{}"

//////////////////////////////////////////////////////////////////////////////////////

## 3 Handling HTTP errors

- Implemented
  => "App.js" (try/catch, useState() to setError)

//////////////////////////////////////////////////////////////////////////////////////

## 4 Using useEffect() for Request

- Implemented
  => "App.js" (useEffect() to fetch data automatically at the end of event cycle)
  (useCallBack() only runs when one of its dependencies update => To avoid infnite loop with useEffect())

//////////////////////////////////////////////////////////////////////////////////////

## 5 Sending POST request

- Implemented
  => "App.js" (using fetch to POST data) in "addMovieHandler()"

-------------------------------------------------- fetch('URL', { method: 'POST' })
